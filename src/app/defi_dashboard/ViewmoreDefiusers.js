"use client"
import { Box, Image, Tab, TabList, TabPanel, TabPanels, Tabs, Text, useColorModeValue, colorMode, Flex, Tooltip, TableContainer, Table, Thead, Tr, Th, Tbody, Td, Spacer, Button } from "@chakra-ui/react";
import { DownloadIcon } from '@chakra-ui/icons'

import React, { use } from "react";

const ViewmoreDefiusers = () => {
    return (
        <Box
        _dark={{ bg: "#191919" }}
        _light={{ bg: " #F0F0F5" }}
        >

        </Box>
    )

}
export default ViewmoreDefiusers;